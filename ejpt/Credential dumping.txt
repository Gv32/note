Credential dumping:

Windows:
	Unattend.xml (file with password)
	load kiwi (metasploit module to dump hash or password)
	upload mimikaz (program to upload to dump password)

Linux:
	/etc/shadow (file with password)
	auxiliary/analyze/crack_linux (crack password)